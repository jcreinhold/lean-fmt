# Design notes

Prompt executors place durable interface decisions, rejected alternatives, source/API investigations,
and unresolved design questions here. A result claim must link any note needed to interpret its
evidence. FIP-SPEC writes `01-model.md`: the frozen in-place-format interface — the guarded publish path
`format` reuses from `ruff-06`, the `--check` preview, the exit-code and report-status semantics, the
CLAUDE.md invariant change, and the reuse-vs-new inventory.
