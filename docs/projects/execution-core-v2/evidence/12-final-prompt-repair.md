# Prompt 12 final-audit repair

Date: 2026-07-16

## Classification

The original final prompt was under-scoped. It requested correctness, architecture, performance,
resource, packaging, and documentation checks but named neither the current suite commands nor a
clean-source packaging operation. It did not require a roadmap-clause matrix, positive boundary
evidence, tracked-source audit, module-system guard, or inspection that tests reach the claimed paths.
Its binary-digest rule also needed a concrete decision for the newly added shared exact-run/service
code.

## Repair

The repaired audit requires a row for every roadmap completion clause and Prompt 12 obligation,
complete active-declaration/caller and tracked-source inspections, all seven current suites, the
module/boundary/deep-module guards, a `git archive` clean-source build/smoke, and a documentation
vocabulary audit.

The current executable differs from Prompt 10's accepted digest because Prompt 11 refactored active
batch fallback and evidence resolution in `LeanFmt.Application`, not merely documentation. The final
audit must therefore run one new monitored cold full-mathlib candidate followed immediately by its
forced worker-free warm check. It must not repeat the formatter-integrated workload because current
rules remain source-input only.

No production source, prompt status, or completion claim changed during this repair. Prompt 12
remains the first unresolved item.
