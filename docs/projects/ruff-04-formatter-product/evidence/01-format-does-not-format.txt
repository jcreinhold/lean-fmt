# RFP-SPEC. The command named `format` does not format: it previews the fixes of selected
# lint rules. Regenerate with the commands shown; each block is verbatim output.

$ lean-fmt rules --json
[{"category":"text","code":"FMT001","defaultEnabled":true,"fixable":true,"input":"source","summary":"remove trailing horizontal whitespace"},{"category":"text","code":"FMT002","defaultEnabled":true,"fixable":true,"input":"source","summary":"require a final newline"}]

# Both rules are input=source, so RulePlan.requiresSyntax (Config.lean:199-200) is false and
# officialArtifacts (Application.lean:569) is never called: no syntax tree is ever built.

$ cat -v -e tests/check/Layout.lean   # lint-clean, layout NOT canonical
module$
$
namespace     Alpha$
$
def layoutValue : Nat := 1$
$
end Alpha$

$ lean-fmt format --root . --json --no-cache tests/check/Layout.lean
{"broken":0,"changed":0,"files":[{"diagnostics":[],"diff":null,"findings":[],"formatted":null,"path":"tests/check/Layout.lean","status":"clean","written":false}],"findings":0,"infrastructureFailures":[],"mode":"format","rejected":0,"written":0}
exit=0

# 'clean', findings=0, changed=0, and no formatted text -- yet LeanFmt.Printer renders
# `namespace Alpha` with exactly one space (Printer.lean:344-348 wholeSpan? -> spaceSeparated;
# :511-515 citing Lean v4.32.0 Command.lean:317-318). format did not look at the layout.

$ grep -rn 'LeanFmt.Printer' --include='*.lean' . | grep -v .lake   # who imports the printer
LeanFmtTest.lean:10:import all LeanFmt.Printer
lakefile.lean:46:`LeanFmt.Doc`, `LeanFmt.Comments`, and `LeanFmt.Printer` are deliberately *not* in the plugin library
lakefile.lean:49:the compiler does needs to render a document, and `LeanFmt.Printer` is the sharpest case: it exists to
lakefile.lean:62:    Glob.one `LeanFmt.Printer
lakefile.lean:126:spaces where `LeanFmt.Printer` renders one (`Printer.lean:511-515`, citing `Command.lean:317-318`).

# Only the test binary. The printer is in LeanFmtCore (lakefile.lean:51-63) and is reachable
# from the application; no product path calls it.
