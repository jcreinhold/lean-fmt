---
code: FMT015
category: unused
tier: semantic
lifecycle: preview
fix: report-only
default: off
---

# FMT015 — report an unused variable or binder

- **Lifecycle:** preview
- **Category:** `unused`
- **Tier:** `semantic`
- **Fix:** report-only
- **Default:** off

A bound variable or binder is never used, surfaced from the compiler's `linter.unusedVariables` diagnostic. Report-only: removing or renaming a binder is not an edit any byte-level or projection fact here can prove safe.

## Path out of preview

Graduates when it produces at least 10 audited true positives with zero false positives on a corpus that is not already `linter.unusedVariables`-clean. mathlib runs that linter, so it cannot supply the evidence: the zero this rule scored across 85 mathlib modules measures mathlib's CI, not this rule.

## Example

Bad:

```lean
def constZero (x : Nat) : Nat := 0
```

## Using this rule

- Select: `--select FMT015` or `--select unused`
- Suppress: `-- lean-fmt: ignore[FMT015]`
