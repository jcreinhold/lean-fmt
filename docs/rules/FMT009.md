---
code: FMT009
category: redundancy
tier: syntax
lifecycle: preview
fix: fixable
default: off
---

# FMT009 — remove a duplicate deriving class

- **Lifecycle:** preview
- **Category:** `redundancy`
- **Tier:** `syntax`
- **Fix:** fixable
- **Default:** off

A `deriving` clause names the same class twice. The safe fix deletes the later instance and its separator, so `deriving Repr, Repr` becomes `deriving Repr`. Safe for the same idempotence reason as FMT008.

## Path out of preview

Graduates on the same condition as FMT008, for duplicate `deriving` classes: at least 10 audited true positives with zero false positives on a corpus that is not attribute-reviewed. Its fix passes the same audit; it found nothing across 85 mathlib modules.

## Example

Bad:

```lean
inductive Color where
  | red
  deriving Repr, Repr
```

Good:

```lean
inductive Color where
  | red
  deriving Repr
```

## Using this rule

- Select: `--select FMT009` or `--select redundancy`
- Suppress: `-- lean-fmt: ignore[FMT009]`
