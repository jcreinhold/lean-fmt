module

/-! The root deliberately exports no application API. Application and test entry points import the
private modules they need directly; ordinary downstream imports see an empty module surface. -/
