import LeanFmt.Capability

/-!
# LeanFmt

Root module of the `LeanFmt` Lean capability package. It re-exports the capability
surface that the Lean-linked worker child loads. At the scaffold stage the surface is
metadata only; parse and edit commands are added in later prompts.
-/
