module

import LeanFmt.Basic

/-! Ünïcödé sits before every defect below, so a codepoint column differs from a byte column. -/

/-- Ärgümënt with redundant nested parentheses after multi-byte text. -/
def ünïcödéValue : Nat := ((1))

attribute [simp, simp] ünïcödéValue
