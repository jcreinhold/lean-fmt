module
/-! Module documentation. -/
-- set_option pp.all true and a redundant ((paren)) named only in this line comment
/- A block comment naming @[simp, simp] and `end` with no open scope. -/
def foo : Nat := 1
