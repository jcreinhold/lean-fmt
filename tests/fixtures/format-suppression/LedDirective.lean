module

-- Why this shape is preserved, written above the directive rather than below it.
-- lean-fmt: format-ignore-next
/-- The docstring belongs to the command, and is inside its syntax range. -/
def ledPreserved(alpha:Nat):Nat:=alpha+1

def ledResumed(beta:Nat):Nat:=beta+1
